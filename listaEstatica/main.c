#include <stdio.h>
#include <stdlib.h>
#include "lista.h"

int main(){

    Lista *li;
    struct aluno dados;
    int matricula;

    li = cria_lista();
    int x = lista_cheia(li);
    if(lista_cheia(li));
    int x = insere_lista_final(li, dados);
    int x = insere_lista_inicio(li, dados);
    int insere_lista_ordenada(li, dados);
    int x = remove_lista_elemento(li, matricula);
    int x = int consuta_lista_pos(li, pos, &dados)

    tamanho_lista(li);
    libera_lista(li);

    return 0;
}